var http = require('http');
var fs = require('fs');
var os = require('os');
var dns = require('dns');
var config = require('./resources/config.json');

module.exports = {
  create: function()
  {

  }
};
